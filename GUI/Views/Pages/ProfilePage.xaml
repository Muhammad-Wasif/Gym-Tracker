<Page x:Class="FitTrack.UI.Views.Pages.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:FitTrack.UI.ViewModels"
      xmlns:ctrl="clr-namespace:FitTrack.UI.Controls"
      Title="Profile">
    <Page.DataContext>
        <vm:ProfileViewModel/>
    </Page.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <!-- Edit Form -->
            <Border Style="{StaticResource DataCard}" Grid.Column="0" Margin="0,0,24,0" VerticalAlignment="Top" x:Name="EditCard">
                <StackPanel>
                    <Grid Width="100" Height="100" HorizontalAlignment="Center" Margin="0,0,0,24">
                        <Ellipse x:Name="AvatarRing" Stroke="{Binding Role, Converter={StaticResource RoleToColorConverter}}" StrokeThickness="2" Opacity="0.5">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="10" Color="{Binding Role, Converter={StaticResource RoleToColorConverter}}" Opacity="0.5"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <Ellipse Width="80" Height="80" Fill="{Binding Role, Converter={StaticResource RoleToColorConverter}}"/>
                        <TextBlock Text="{Binding InitialsText}" FontSize="28" FontWeight="Bold" FontFamily="{StaticResource DMSans}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>

                    <StackPanel Margin="0,0,0,12">
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Style="{StaticResource TextInput}" Tag="Full Name" Text="{Binding FullName}" Margin="0,0,8,0"/>
                            <TextBox Style="{StaticResource TextInput}" Tag="Email" Text="{Binding Email}" Grid.Column="1" Margin="8,0,0,0"/>
                        </Grid>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox SelectedValue="{Binding Gender}" SelectedValuePath="Content" Margin="0,0,8,0">
                                <ComboBoxItem Content="Male"/>
                                <ComboBoxItem Content="Female"/>
                                <ComboBoxItem Content="Other"/>
                            </ComboBox>
                            <TextBox Style="{StaticResource TextInput}" Tag="Age" Text="{Binding Age}" Grid.Column="1" Margin="8,0,0,0"/>
                        </Grid>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Style="{StaticResource TextInput}" Tag="Height (cm)" Text="{Binding HeightCm}" Margin="0,0,8,0"/>
                            <TextBox Style="{StaticResource TextInput}" Tag="Weight (kg)" Text="{Binding WeightKg}" Grid.Column="1" Margin="8,0,0,0"/>
                        </Grid>
                        <TextBox Style="{StaticResource TextInput}" Tag="Body Fat %" Text="{Binding BodyFatPct}" Margin="0,0,0,12"/>
                        <ComboBox ItemsSource="{Binding Goals}" SelectedItem="{Binding SelectedGoal}" DisplayMemberPath="GoalName" Margin="0,0,0,16"/>
                    </StackPanel>

                    <Button Style="{StaticResource PrimaryButton}" Content="Save Changes" Command="{Binding SaveProfileCommand}" HorizontalAlignment="Left" Padding="20,12"/>

                    <Rectangle Height="1" Fill="{StaticResource BorderBrush}" Margin="0,32,0,24"/>
                    <TextBlock Text="Change Password" Style="{StaticResource SectionTitle}" Margin="0,0,0,16"/>
                    
                    <PasswordBox x:Name="PbCurrent" Style="{StaticResource PasswordInput}" Tag="Current Password" Margin="0,0,0,12" PasswordChanged="PbCurrent_PasswordChanged"/>
                    <PasswordBox x:Name="PbNew" Style="{StaticResource PasswordInput}" Tag="New Password" Margin="0,0,0,12" PasswordChanged="PbNew_PasswordChanged"/>
                    <PasswordBox x:Name="PbConfirm" Style="{StaticResource PasswordInput}" Tag="Confirm New Password" Margin="0,0,0,16" PasswordChanged="PbConfirm_PasswordChanged"/>
                    
                    <Button Style="{StaticResource GhostButton}" Content="Update Password" Command="{Binding ChangePasswordCommand}" HorizontalAlignment="Left"/>
                    
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource RedBrush}" Margin="0,16,0,0"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
                    <TextBlock Text="{Binding SuccessMessage}" Foreground="{StaticResource GreenBrush}" Margin="0,16,0,0"
                               Visibility="{Binding SuccessMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
                </StackPanel>
            </Border>

            <!-- Stats Card -->
            <Border Style="{StaticResource DataCard}" Grid.Column="1" VerticalAlignment="Top" x:Name="StatsCard">
                <StackPanel>
                    <TextBlock Text="{Binding Username, StringFormat=@{0}}" Style="{StaticResource MonoSmall}" Foreground="{StaticResource BrandBrush}" Margin="0,0,0,8"/>
                    <ctrl:GoalBadge GoalName="{Binding Role}" HorizontalAlignment="Left" Margin="0,0,0,24"/>
                    
                    <Rectangle Height="1" Fill="{StaticResource BorderBrush}" Margin="0,0,0,24"/>
                    
                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{Binding BMI, StringFormat={}{0:0.0}}" Style="{StaticResource CardTitle}"/>
                            <TextBlock Text="{Binding BMICategory}" Style="{StaticResource Caption}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,0,16">
                            <TextBlock Text="{Binding BMR, StringFormat={}{0:0}}" Style="{StaticResource CardTitle}"/>
                            <TextBlock Text="kcal/day at rest" Style="{StaticResource Caption}"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="1">
                            <TextBlock Text="{Binding TDEE, StringFormat={}{0:0}}" Style="{StaticResource CardTitle}"/>
                            <TextBlock Text="kcal/day (moderate)" Style="{StaticResource Caption}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="1">
                            <TextBlock Text="{Binding TargetCalories, StringFormat={}{0:0}}" Style="{StaticResource CardTitle}"/>
                            <TextBlock Text="kcal/day goal" Style="{StaticResource Caption}"/>
                        </StackPanel>
                    </Grid>
                    
                    <Rectangle Height="1" Fill="{StaticResource BorderBrush}" Margin="0,0,0,16"/>
                    <TextBlock Text="{Binding MemberSince, StringFormat=Member since {0}}" Style="{StaticResource Caption}" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal" Visibility="{Binding TrainerName, Converter={StaticResource NullToVisibilityConverter}}">
                        <TextBlock Text="👤" Foreground="{StaticResource TextSecBrush}" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding TrainerName}" Style="{StaticResource BodyText}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
